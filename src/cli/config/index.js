import config from './abe-config'
import configLocal from './abe-config-local'

var abeConfig = config
var abeConfigLocal = configLocal

export {config as abeConfig}
export {configLocal as abeConfigLocal}